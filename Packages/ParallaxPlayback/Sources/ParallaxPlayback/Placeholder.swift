import Foundation
import ParallaxCore

// Module anchor: SwiftPM rejects an empty target. Deleted in Task 4a.2 once
// the first real type (PlaybackEngineID et al.) lands in Engine/.
enum ParallaxPlaybackPlaceholder {}
